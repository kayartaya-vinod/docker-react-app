language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t learnwithvinod/react-app:latest .

script:
    - docker run -e CI=true learnwithvinod/react-app:latest npm run test