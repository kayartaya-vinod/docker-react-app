language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t learnwithvinod/react-app:for-test -f Dockerfile.dev .

script:
    - docker run -e CI=true learnwithvinod/react-app:for-test npm test

deploy:
    provider: elasticbeanstalk
    region: "us-east-2"
    app: "docker-react-app"
    env: "DockerReactApp-env"
    bucket_name: "elasticbeanstalk-us-east-2-575452076059"
    bucket_path: "docker-react-app"
    access-key-id: "AKIAYL6453ANTEON2R5Q"
    secret-access-key: "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"
    skip_cleanup: "true"
    on:
        branch: master
    
